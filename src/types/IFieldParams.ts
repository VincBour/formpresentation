export interface IFieldParams {
    name: {
        label: string,
        required: boolean,
        readOnly: boolean,
        placeHolder?: string
    },
    email: {
        label: string,
        required: boolean,
        readOnly: boolean,
        placeHolder?: string
    }
}